apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw
spec:
  type: ClusterIP
  ports:
    - port: 18789
      targetPort: gateway
      protocol: TCP
      name: gateway
  selector:
    app.kubernetes.io/name: openclaw
