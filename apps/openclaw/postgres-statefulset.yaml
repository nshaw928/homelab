apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: openclaw-postgres
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw-postgres
    app.kubernetes.io/component: database
spec:
  serviceName: openclaw-postgres
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: openclaw-postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: openclaw-postgres
        app.kubernetes.io/component: database
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP
          env:
            - name: POSTGRES_USER
              value: openclaw
            - name: POSTGRES_DB
              value: openclaw_studio
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: openclaw-db-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGREST_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: openclaw-db-secrets
                  key: POSTGREST_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: init-schema
              mountPath: /docker-entrypoint-initdb.d
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - openclaw
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - openclaw
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: init-schema
          configMap:
            name: openclaw-init-schema
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: longhorn
        resources:
          requests:
            storage: 5Gi
