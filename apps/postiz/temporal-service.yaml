apiVersion: v1
kind: Service
metadata:
  name: temporal
  namespace: postiz
  labels:
    app.kubernetes.io/name: temporal
    app.kubernetes.io/component: workflow
spec:
  type: ClusterIP
  ports:
    - port: 7233
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    app.kubernetes.io/name: temporal
