apiVersion: v1
kind: Secret
metadata:
  name: linkwarden-secrets
  namespace: linkwarden
  labels:
    app.kubernetes.io/name: linkwarden
type: Opaque
stringData:
  # PLACEHOLDER - Do not use these values!
  # Before ArgoCD syncs, create the real secret manually:
  #
  # export KUBECONFIG=/home/nick/homelab/ansible/kubeconfig.yaml
  # NEXTAUTH_SECRET=$(openssl rand -base64 32)
  # POSTGRES_PASSWORD=$(openssl rand -base64 24)
  # kubectl create namespace linkwarden
  # kubectl create secret generic linkwarden-secrets \
  #   --namespace linkwarden \
  #   --from-literal=NEXTAUTH_SECRET="$NEXTAUTH_SECRET" \
  #   --from-literal=POSTGRES_PASSWORD="$POSTGRES_PASSWORD"
  #
  NEXTAUTH_SECRET: "REPLACE_ME_BEFORE_DEPLOY"
  POSTGRES_PASSWORD: "REPLACE_ME_BEFORE_DEPLOY"
