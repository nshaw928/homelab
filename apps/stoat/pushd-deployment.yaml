apiVersion: apps/v1
kind: Deployment
metadata:
  name: stoat-pushd
  namespace: stoat
  labels:
    app.kubernetes.io/name: stoat-pushd
    app.kubernetes.io/component: backend
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  revisionHistoryLimit: 3
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: stoat-pushd
  template:
    metadata:
      labels:
        app.kubernetes.io/name: stoat-pushd
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: pushd
          image: ghcr.io/stoatchat/pushd:v0.11.0
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - name: config
              mountPath: /Revolt.toml
              subPath: Revolt.toml
              readOnly: true
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
      volumes:
        - name: config
          secret:
            secretName: stoat-secrets
            items:
              - key: Revolt.toml
                path: Revolt.toml
