apiVersion: v1
kind: Service
metadata:
  name: stoat-caddy
  namespace: stoat
  labels:
    app.kubernetes.io/name: stoat-caddy
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: stoat-caddy
