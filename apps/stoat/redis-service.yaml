apiVersion: v1
kind: Service
metadata:
  name: stoat-redis
  namespace: stoat
  labels:
    app.kubernetes.io/name: stoat-redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app.kubernetes.io/name: stoat-redis
