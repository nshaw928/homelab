apiVersion: v1
kind: Service
metadata:
  name: stoat-events
  namespace: stoat
  labels:
    app.kubernetes.io/name: stoat-events
spec:
  type: ClusterIP
  ports:
    - port: 14703
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: stoat-events
