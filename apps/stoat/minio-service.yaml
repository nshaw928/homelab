apiVersion: v1
kind: Service
metadata:
  name: stoat-minio
  namespace: stoat
  labels:
    app.kubernetes.io/name: stoat-minio
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: stoat-minio
