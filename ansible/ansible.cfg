[defaults]
# Inventory file location
inventory = inventory/hosts.yml

# SSH settings
remote_user = nick
private_key_file = ~/.ssh/id_ed25519

# Disable host key checking (for lab only!)
host_key_checking = False

# Speed improvements
forks = 10
pipelining = True

# Output formatting
stdout_callback = default
result_format = yaml
